//test for memset function

#include <stdio.h>
#include <string.h>
#include <memory.h>

int main()
{
	int i,cnt[255];
	memset(cnt,0,sizeof(cnt));
	for(i=0;i<255;i++)
			printf("%d\n",cnt[i]);
//	char buffer[]="Hello world\n";
//	printf("Buffer before memset: %s\n",buffer);
//	memset(buffer,'*',strlen(buffer));
//	printf("Buffer after memset:%s\n",buffer);
	return 0;
	
}
