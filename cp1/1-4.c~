//the celsius to fahrenheit temperature table
#include <stdio.h>

main()
{
	float fahr,celsius;
	int lower,upper,step;

	lower = -40;
	upper =100 ;
	step = 20;
	celsius = lower;
	printf("#Celsius To Fahrenheit Temperature Comparation#\n");
	while(celsius <= upper)
	{
		fahr = (celsius*9.0)/5.0 + 32.0;
		printf("%3.0f %6.1f\n",celsius,fahr);
		celsius = celsius + step;
	}
}
